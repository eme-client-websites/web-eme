---
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Enlaces del menú - exactamente igual al menú de desktop
const menuItems = [
  { href: '#servicios', text: lang === 'en' ? 'Services' : 'Servicios' },
  { href: '#proyectos', text: lang === 'en' ? 'Work' : 'Trabajos' },
  { href: '#contacto', text: lang === 'en' ? 'Contact' : 'Contacto' }
];

// Configuración del selector de idioma
const currentLang = getLangFromUrl(Astro.url);
const otherLang = currentLang === 'es' ? 'en' : 'es';

// Construir la URL para el otro idioma
function getOtherLangUrl() {
  const pathname = Astro.url.pathname;
  
  if (currentLang === 'es') {
    // Si estamos en español, agregar /en al inicio
    return '/en' + pathname;
  } else {
    // Si estamos en inglés, quitar /en del inicio
    return pathname.replace('/en', '') || '/';
  }
}

const otherLangUrl = getOtherLangUrl();
---

<!-- Header móvil - Solo visible en dispositivos móviles -->
<header class="mobile-header fixed top-0 left-0 right-0 z-50 lg:hidden bg-white/10 backdrop-blur-sm border-b border-white/10">
  <div class="flex items-center justify-between px-4 py-3">
    
    <!-- Logo -->
    <div class="flex-shrink-0">
      <a href="/" class="flex items-center">
        <img src="/logo-emeblanco.svg" alt="E-ME Agency" class="h-8 w-auto">
      </a>
    </div>

    <!-- Selector de idioma y botón hamburguesa -->
    <div class="flex items-center space-x-3">
      <!-- Selector de idioma compacto -->
      <div class="flex items-center justify-center bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-3 py-2 h-8">
        <!-- Idioma actual -->
        <div class="flex items-center justify-center w-4 h-4">
          <span class="text-white font-medium text-xs leading-none text-center">
            {currentLang === 'es' ? 'ES' : 'EN'}
          </span>
        </div>
        
        <div class="flex items-center justify-center w-2 h-4">
          <span class="text-white/40 text-xs leading-none text-center">|</span>
        </div>
        
        <!-- Idioma alternativo -->
        <a 
          href={otherLangUrl}
          class="hover:scale-110 transition-all duration-300 opacity-60 hover:opacity-100 flex items-center justify-center w-4 h-4"
        >
          <span class="text-white font-medium text-xs leading-none text-center">
            {otherLang === 'es' ? 'ES' : 'EN'}
          </span>
        </a>
      </div>

      <!-- Botón hamburguesa -->
      <button class="mobile-hamburger-toggle flex flex-col items-center justify-center w-8 h-8 space-y-1 focus:outline-none" aria-label="Abrir menú">
        <span class="mobile-hamburger-line w-6 h-0.5 bg-white transition-all duration-300 ease-in-out"></span>
        <span class="mobile-hamburger-line w-6 h-0.5 bg-white transition-all duration-300 ease-in-out"></span>
        <span class="mobile-hamburger-line w-6 h-0.5 bg-white transition-all duration-300 ease-in-out"></span>
      </button>
    </div>

  </div>

  <!-- Menú móvil desplegable -->
  <div class="mobile-menu fixed inset-0 bg-white transform translate-y-full transition-transform duration-300 ease-in-out z-50 hidden">
    <!-- Header del menú con botón cerrar -->
    <div class="flex justify-between items-center p-4 border-b">
      <img src="/logo-emeblanco.svg" alt="E-ME Agency" class="h-8 w-auto filter invert">
      
      <!-- Botón X para cerrar -->
      <button class="mobile-menu-close w-10 h-10 flex items-center justify-center rounded-full hover:bg-gray-100 transition-colors">
        <svg class="w-6 h-6 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>

    <!-- Contenido del menú optimizado para móvil -->
    <div class="flex flex-col h-full overflow-y-auto">
      
      <!-- Enlaces del menú -->
      <div class="flex-1 px-6 py-6">
        <ul class="space-y-4">
          {menuItems.map((item) => (
            <li>
              <a 
                href={item.href}
                class="mobile-menu-link block text-black hover:text-blue-600 transition-colors duration-300 text-2xl font-bold py-2 border-b border-gray-100"
              >
                {item.text}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Información de contacto y redes -->
      <div class="bg-gray-50 px-6 py-6 space-y-4">
        
        <!-- Redes sociales -->
        <div>
          <p class="text-xs text-gray-600 mb-3 text-center">Síguenos</p>
          <div class="flex justify-center gap-3">
            <a href="https://instagram.com/eme.agency" target="_blank" class="w-9 h-9 bg-gray-300 hover:bg-blue-500 hover:text-white rounded-full flex items-center justify-center text-black transition-all">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.40s-.644-1.44-1.439-1.40z"/>
              </svg>
            </a>
            <a href="https://facebook.com/eme.agency" target="_blank" class="w-9 h-9 bg-gray-300 hover:bg-blue-500 hover:text-white rounded-full flex items-center justify-center text-black transition-all">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
              </svg>
            </a>
            <a href="https://twitter.com/eme_agency" target="_blank" class="w-9 h-9 bg-gray-300 hover:bg-blue-500 hover:text-white rounded-full flex items-center justify-center text-black transition-all">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
              </svg>
            </a>
            <a href="https://linkedin.com/company/e-me-agency" target="_blank" class="w-9 h-9 bg-gray-300 hover:bg-blue-500 hover:text-white rounded-full flex items-center justify-center text-black transition-all">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
              </svg>
            </a>
            <a href="https://youtube.com/@eme-agency" target="_blank" class="w-9 h-9 bg-gray-300 hover:bg-blue-500 hover:text-white rounded-full flex items-center justify-center text-black transition-all">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
              </svg>
            </a>
          </div>
        </div>
        
        <!-- Información de contacto -->
        <div class="text-center space-y-2">
          <div>
            <p class="text-xs text-gray-600">Consultas</p>
            <p class="font-medium text-xs">info@eme-agency.com</p>
          </div>
          <div>
            <p class="text-xs text-gray-600">Teléfono</p>
            <p class="font-medium text-xs">+57 123 456 789</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<script>
  // Funcionalidad del menú móvil - mejorada
  function initMobileMenu() {
    const menuToggle = document.querySelector('.mobile-hamburger-toggle');
    const mobileMenu = document.querySelector('.mobile-menu');
    const menuClose = document.querySelector('.mobile-menu-close');
    const hamburgerLines = document.querySelectorAll('.mobile-hamburger-line');
    const menuLinks = document.querySelectorAll('.mobile-menu-link');
    
    let isMenuOpen = false;

    function openMenu() {
      isMenuOpen = true;
      mobileMenu?.classList.remove('hidden');
      mobileMenu?.classList.remove('translate-y-full');
      mobileMenu?.classList.add('translate-y-0');
      
      // Animar hamburguesa a X
      if (hamburgerLines.length >= 3) {
        (hamburgerLines[0] as HTMLElement).style.transform = 'rotate(45deg) translate(2px, 6px)';
        (hamburgerLines[0] as HTMLElement).style.backgroundColor = '#000000';
        (hamburgerLines[1] as HTMLElement).style.opacity = '0';
        (hamburgerLines[2] as HTMLElement).style.transform = 'rotate(-45deg) translate(2px, -6px)';
        (hamburgerLines[2] as HTMLElement).style.backgroundColor = '#000000';
      }
      
      // Prevenir scroll del body
      document.body.style.overflow = 'hidden';
    }

    function closeMenu() {
      isMenuOpen = false;
      mobileMenu?.classList.remove('translate-y-0');
      mobileMenu?.classList.add('translate-y-full');
      
      // Ocultar completamente después de la animación
      setTimeout(() => {
        mobileMenu?.classList.add('hidden');
      }, 300);
      
      // Restaurar hamburguesa
      if (hamburgerLines.length >= 3) {
        (hamburgerLines[0] as HTMLElement).style.transform = 'none';
        (hamburgerLines[0] as HTMLElement).style.backgroundColor = '#ffffff';
        (hamburgerLines[1] as HTMLElement).style.opacity = '1';
        (hamburgerLines[2] as HTMLElement).style.transform = 'none';
        (hamburgerLines[2] as HTMLElement).style.backgroundColor = '#ffffff';
      }
      
      // Restaurar scroll del body
      document.body.style.overflow = '';
    }

    // Toggle menú al hacer click en hamburguesa
    menuToggle?.addEventListener('click', () => {
      if (isMenuOpen) {
        closeMenu();
      } else {
        openMenu();
      }
    });

    // Cerrar menú al hacer click en X
    menuClose?.addEventListener('click', () => {
      closeMenu();
    });

    // Cerrar menú al hacer click en enlaces
    menuLinks.forEach(link => {
      link.addEventListener('click', () => {
        closeMenu();
        // Pequeño delay para que se vea la animación
        setTimeout(() => {
          const href = link.getAttribute('href');
          if (href && href.startsWith('#')) {
            const target = document.querySelector(href);
            if (target) {
              target.scrollIntoView({ behavior: 'smooth' });
            }
          }
        }, 300);
      });
    });

    // Cerrar menú con tecla ESC
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && isMenuOpen) {
        closeMenu();
      }
    });
  }

  // Inicializar cuando el DOM esté listo
  document.addEventListener('DOMContentLoaded', initMobileMenu);
</script>

<style>
  /* Asegurar que el header móvil esté por encima de todo */
  .mobile-header {
    z-index: 9999;
  }
  
  /* Estilos para el menú móvil */
  .mobile-menu {
    z-index: 9998;
    width: 100vw;
    height: 100vh;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
  }
  
  /* Animación suave para las líneas del hamburguesa móvil */
  .mobile-hamburger-line {
    transform-origin: center;
  }
  
  /* Efectos hover mejorados para los enlaces del menú */
  .mobile-menu-link {
    position: relative;
  }
  
  .mobile-menu-link span {
    display: inline-block;
    transition: transform 0.3s ease;
  }
  
  /* Adaptación responsive */
  @media (max-width: 640px) {
    .mobile-menu-link {
      font-size: 2.5rem;
    }
    
    .mobile-menu .flex-col {
      padding: 2rem 1rem;
    }
  }
</style>